<%= form_for(@long_joke) do |f| %>
  <% if @long_joke.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@long_joke.errors.count, "error") %> prohibited this long_joke from being saved:</h2>

      <ul>
      <% @long_joke.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contents %><br>
    <%= f.text_area :contents, :size => "60x10" %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.select(:user_id, options_from_collection_for_select(User.all, :id, :name)) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
